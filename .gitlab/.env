# GitLab CI Environment Variables
# Maps GitLab CI context to project variables

# GitLab-specific CI Variables
BUILD_ENV=ci
CI_PROVIDER=gitlab
CI_BUILD_ID=${CI_JOB_ID}
CI_BUILD_NUMBER=${CI_PIPELINE_ID}
CI_JOB_NAME=${CI_JOB_NAME}
CI_PIPELINE_SOURCE=${CI_PIPELINE_SOURCE}

# Source Control (GitLab provides these natively)
# CI_COMMIT_SHA - provided by GitLab
# CI_COMMIT_REF_NAME - provided by GitLab
# CI_COMMIT_BRANCH - provided by GitLab
# CI_COMMIT_TAG - provided by GitLab

# Registry Configuration (GitLab provides these natively)
# CI_REGISTRY - provided by GitLab
# CI_REGISTRY_USER - provided by GitLab
# CI_REGISTRY_PASSWORD - provided by GitLab
# CI_REGISTRY_IMAGE - provided by GitLab

# Publishing
PUBLISH_REGISTRY=${CI_REGISTRY}
PUBLISH_IMAGE=${CI_REGISTRY_IMAGE}/${PROJECT_NAME}

# Artifacts
ARTIFACT_REGISTRY=${CI_REGISTRY}
ARTIFACT_PATH=artifacts

# GitLab Pages
PAGES_URL=${CI_PAGES_URL}
PAGES_DIR=public